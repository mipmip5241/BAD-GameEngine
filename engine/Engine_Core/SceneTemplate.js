/**
 * @abstract Scene file template, each scene file must extends this class.
 */

class SceneTemplate
{
    initialize() { throw "this is an abstract class";}
    loadScene() {throw "this is an abstract class";}
    unloadScene() {throw "this is an abstract class";}
}